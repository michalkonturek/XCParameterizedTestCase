language: objective-c
before_install:
    
    - gem install cocoapods --no-ri --no-rdoc
    - gem install xcpretty --no-ri --no-rdoc

    - pod --version

    - cd Example

script: make ci

notifications:
    email: false
